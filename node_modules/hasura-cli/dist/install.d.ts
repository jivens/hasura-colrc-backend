export declare function versionFromPackageJson(): string;
export declare function tagFromVersion(version: string): string;
interface InstallOptions {
    version?: string;
    destDir?: string;
    fileName?: string;
    platform?: string;
    verbose?: boolean;
}
export declare const defaultInstallOptions: {
    destDir: string;
    fileName: string;
    dest(): string;
};
export declare function install({ version, destDir, fileName, platform, verbose, }?: InstallOptions): Promise<string>;
export {};
